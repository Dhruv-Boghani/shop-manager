<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Error</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container py-5">
    <div class="text-center border rounded p-4 shadow bg-white">
      <h1 class="text-danger">Something went wrong!</h1>
      <p class="fs-5 text-muted"><%= message || "An unexpected error occurred." %></p>

      <% if (error && error.stack) { %>
        <pre class="text-start bg-light p-3 border rounded mt-4 overflow-auto" style="max-height: 300px;">
<%= error.stack %>
        </pre>
      <% } %>

      <a href="/" class="btn btn-primary mt-4">Go to Home</a>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>